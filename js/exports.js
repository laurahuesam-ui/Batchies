function csvEscape(v){const s=String(v??'');return /[;"\n]/.test(s)?'"'+s.replace(/"/g,'""')+'"':s}
function downloadText(name,text,type='text/plain;charset=utf-8'){const blob=new Blob(['\ufeff'+text],{type}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)}
function exportProductsCsv(){const h=['PID','Produktname','Hauptkategorie','Unterkategorie','Status','Kalkulationspreis','Lieferantenanzahl','Bevorzugter Lieferant','Lieferanten','URLs','Lieferantenpreise','Mindestbestellmengen','Lieferanten-Versand gesamt','Lieferanten-Versand pro Stück','Lieferanten-Bild-URLs','Produkt-Bild-URL','Weitere Kosten','Versandkosten','Kundenversand','Zielmarge %','Verkaufspreis','Offsite Ads','Währungsumrechnung','Setup-Umlage','Notizen'];const rows=state.products.map(p=>{const suppliers=p.suppliers||[],preferred=suppliers.find(s=>s.preferred);return[p.pid,p.name,p.category,p.subcategory||'',statusLabel(p.status),p.basePrice,suppliers.length,preferred?.name||'',suppliers.map(s=>(s.preferred?'★ ':'')+s.name).join(' | '),suppliers.map(s=>s.url).join(' | '),suppliers.map(s=>s.price).join(' | '),suppliers.map(s=>Math.max(1,num(s.minOrderQty,1))).join(' | '),suppliers.map(s=>num(s.totalShipping)).join(' | '),suppliers.map(s=>supplierUnitShipping(s).toFixed(2)).join(' | '),suppliers.map(s=>s.imageUrl||'').join(' | '),p.imageUrl,(p.costs||[]).map(c=>c.name+': '+c.amount).join(' | '),p.shippingCost,p.shippingCharged,p.targetMargin,p.salePrice,p.useOffsite?'Ja':'Nein',p.useCurrency?'Ja':'Nein',p.useSetup?'Ja':'Nein',p.notes]});downloadText('batchies-produkte-'+new Date().toISOString().slice(0,10)+'.csv',[h,...rows].map(r=>r.map(csvEscape).join(';')).join('\n'),'text/csv;charset=utf-8')}
function exportBatchesCsv(){const h=['BID','Batchname','Status','Produkte (PID x Menge)','Produktkosten','Zusatzkosten','Gesamtkosten','Geplanter Verkaufspreis','Notizen'];const rows=state.batches.map(b=>{const c=batchCalc(b);return[b.bid,b.name,statusLabel(b.status),(b.items||[]).map(i=>i.pid+' x '+i.qty).join(' | '),c.productCost,b.extraCost,c.total,b.salePrice,b.notes]});downloadText('batchies-batches-'+new Date().toISOString().slice(0,10)+'.csv',[h,...rows].map(r=>r.map(csvEscape).join(';')).join('\n'),'text/csv;charset=utf-8')}
function exportBackup(){downloadText('batchies-backup-'+new Date().toISOString().slice(0,10)+'.json',JSON.stringify(state,null,2),'application/json')}
